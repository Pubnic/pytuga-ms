{
    "name": "pytuga-ms",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "install:dev": "sls requirements install",
        "start:dev": "sls invoke local --aws-profile pubnic",
        "deploy:dev": "sls deploy --aws-profile pubnic"
    },
    "dependencies": {},
    "devDependencies": {
        "serverless-python-requirements": "^5.1.0"
    }
}
